import React from 'react'
import Cards from "../cards"

const BeerDisplay = (props) => {
    return (
        <div>
            <Cards props={props}/>
            
        </div>
    )
}

export default BeerDisplay
